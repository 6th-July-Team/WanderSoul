using UnityEngine;

public enum ConvoyFailReason
{
    None,
    PlayerDefeated,
    WagonDestroyed,
    OutOfWagonArea,   
}
public class ConvoyResult : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
